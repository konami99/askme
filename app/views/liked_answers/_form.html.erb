<%= form_for(@liked_answer) do |f| %>
  <% if @liked_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liked_answer.errors.count, "error") %> prohibited this liked_answer from being saved:</h2>

      <ul>
      <% @liked_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer_id %><br>
    <%= f.text_field :answer_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
